<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="#2a2e4c">
    <title>Reset Password - Your Account</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <style>
        body {
            background-color: #2a2e4c;
            font-family: 'Poppins', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            color: #fff;
            line-height: 1.6;
        }

        .container {
            background: #fff;
            padding: 2.5rem;
            border-radius: 15px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 450px;
            text-align: center;
            animation: fadeIn 0.5s ease-in;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .container h2 {
            color: #2a2e4c;
            font-size: 1.8rem;
            margin-bottom: 1.2rem;
        }

        .container p {
            color: #666;
            margin-bottom: 1.8rem;
            font-size: 0.95rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
            text-align: left;
            position: relative;
        }

        .form-group label {
            color: #2a2e4c;
            display: block;
            margin-bottom: 0.6rem;
            font-weight: 600;
        }

        .form-group input {
            width: 100%;
            padding: 0.7rem 40px 0.7rem 0.7rem;
            border: 2px solid #ddd;
            border-radius: 8px;
            font-size: 1rem;
            transition: border-color 0.3s ease;
            box-sizing: border-box;
        }

        .form-group input:focus {
            border-color: #2a2e4c;
            outline: none;
        }

        .toggle-password {
            position: absolute;
            right: 15px;
            top: 60px;
            /* Slight adjustment from 50% to better align with text baseline */
            transform: translateY(-50%);
            /* Center relative to the adjusted top */
            cursor: pointer;
            width: 20px;
            height: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
        }



        .btn {
            background-color: #2a2e4c;
            color: #fff;
            padding: 0.9rem;
            border: none;
            border-radius: 8px;
            width: 100%;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .btn:hover {
            background-color: #1e223a;
        }

        .link {
            color: #2a2e4c;
            text-decoration: none;
            margin-top: 1.2rem;
            display: block;
            font-size: 0.9rem;
            transition: color 0.3s ease;
        }

        .link:hover {
            color: #1e223a;
        }
    </style>
</head>

<body>
    <div class="container">
        <h2>Reset Password</h2>
        <p>Create a new password for your account.</p>
        <% if (error && error.length> 0) { %>
            <div style="
                background-color: #fef2f2;
                color: #b91c1c;
                border: 1px solid #fca5a5;
                padding: 12px 16px;
                border-radius: 8px;
                font-size: 15px;
                font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
                margin-bottom: 20px;
                display: flex;
                align-items: center;
                gap: 10px;
              ">
                <span style="font-size: 18px;"></span>
                <span>
                    <%= error[0] %>
                </span>
            </div>
            <% } %>

                <form action="/reset-password" method="post">
                    <div class="form-group">
                        <label for="newPassword">New Password</label>
                        <input type="password" id="newPassword" name="newPassword" placeholder="Enter new password">
                        <span class="toggle-password" onclick="togglePassword('newPassword')">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#2a2e4c"
                                stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z" />
                                <circle cx="12" cy="13" r="3" /> <!-- Adjusted cy from 12 to 13 -->
                            </svg>
                        </span>
                    </div>
                    <div class="form-group">
                        <label for="confirmPassword">Confirm Password</label>
                        <input type="password" id="confirmPassword" name="confirmPassword"
                            placeholder="Confirm new password">
                        <span class="toggle-password" onclick="togglePassword('newPassword')">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#2a2e4c"
                                stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z" />
                                <circle cx="12" cy="13" r="3" /> <!-- Adjusted cy from 12 to 13 -->
                            </svg>
                        </span>
                    </div>
                    <button type="submit" class="btn">Reset Password</button>
                </form>
                <a href="/login" class="link">Back to Sign In</a>
    </div>

    <script>
        function togglePassword(fieldId) {
            const passwordField = document.getElementById(fieldId);
            const toggleIcon = passwordField.nextElementSibling.querySelector('svg');
            if (passwordField.type === "password") {
                passwordField.type = "text";
                toggleIcon.style.transform = "scale(1.2)";
            } else {
                passwordField.type = "password";
                toggleIcon.style.transform = "scale(1)";
            }
        }
    </script>
</body>

</html>